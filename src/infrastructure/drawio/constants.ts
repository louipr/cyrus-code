/**
 * Draw.io Constants
 *
 * Default values for Draw.io diagram parsing.
 */

/** Default width for diagram elements without explicit geometry */
export const DEFAULT_ELEMENT_WIDTH = 100;

/** Default height for diagram elements without explicit geometry */
export const DEFAULT_ELEMENT_HEIGHT = 60;

/** Default position for diagram elements without explicit geometry */
export const DEFAULT_POSITION_X = 0;
export const DEFAULT_POSITION_Y = 0;
