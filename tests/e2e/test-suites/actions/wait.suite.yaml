description: Test wait action.
metadata:
  status: draft
  tags:
    - actions
    - wait
steps:
  - action: wait
    timeout: 2000
    expect:
      selector: "#root"
      assert: exists
    why: Wait for root element. Success means wait found the element.
