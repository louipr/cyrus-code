description: Save the current diagram to disk

metadata:
  status: draft
  reliability: medium
  tags: [drawio, file, save]
  author: macro-generator
  created: "2026-01-27"
  modified: "2026-01-27"

context:
  app: cyrus-code
  prerequisites:
    - App is launched
    - Diagram view is open with content

steps:
  - action: wait
    timeout: 1000
    expect:
      selector: "[data-testid='diagram-webview']"
      assert: exists
    why: Verify diagram view is open

  - action: wait
    timeout: 1000
    why: Wait for diagram to be ready before save operation
