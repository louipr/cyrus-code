description: Test evaluate action.
metadata:
  status: draft
  tags:
    - actions
    - evaluate
steps:
  - action: evaluate
    code: |
      return { answer: 42 };
    timeout: 1000
    expect:
      assert: equals
      expected: { answer: 42 }
    why: Evaluate JS and verify return value.
