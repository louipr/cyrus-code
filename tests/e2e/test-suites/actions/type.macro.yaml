description: Test type action.
metadata:
  status: draft
  tags:
    - actions
    - type
steps:
  - action: click
    selector: "[data-testid='symbols-view-button']"
    timeout: 2000
    expect:
      selector: "[data-testid='search-input']"
      assert: exists
    why: Switch to symbols view where search input is visible.
  - action: type
    selector: "[data-testid='search-input']"
    text: test
    timeout: 2000
    expect:
      selector: "[data-testid='search-clear']"
      assert: exists
    why: Type in search input and verify clear button appears via selector assertion.
